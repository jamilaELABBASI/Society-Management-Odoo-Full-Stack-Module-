<t t-name="society_management.project_form">
    <div class="form_container">
        <div class="form">
            <h2>
                <t t-if="props.state.projectMode == 'create'">Create Project</t>
                <t t-else="">Edit Project</t>
            </h2>
            <input type="text"
                   placeholder="Project Name"
                   t-model="props.state.projectForm.name"/>

            <textarea placeholder="Description"
                      t-model="props.state.projectForm.description"/>

            <select t-model="props.state.projectForm.manager_id">
                <option value="false">Select Manager</option>
                <t t-if="props.users">
                    <t t-foreach="props.users" t-as="user" t-key="user.id">
                    <option t-att-value="user.id">
                        <t t-esc="user.name"/>
                    </option>
                </t>
                </t>
            </select>

            <div class="dates">
                    <div>
                        <h3>Start date</h3>
                        <input type="date" t-model="props.state.projectForm.start_date"/>
                    </div>
                    <div>
                        <h3>End date</h3>
                        <input type="date" t-model="props.state.projectForm.end_date"/>
                    </div>
            </div>

            <select t-model="props.state.projectForm.state">
                <option value="draft">Draft</option>
                <option value="progress">In Progress</option>
                <option value="done">Done</option>
                <option value="cancel">Cancelled</option>
            </select>

            <div class="buttons">
                <button class="button_save" t-on-click="props.saveProject">Save</button>
                <button class="button_cancel" t-on-click="props.hideProjectForm">Cancel</button>
            </div>

        </div>
    </div>
</t>